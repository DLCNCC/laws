1. **创建百度统计代码partial文件**：

   * 创建 `layouts/partials/baidu_analytics.html` 文件

   * 在文件中添加百度统计JavaScript代码

   * 确保代码只在生产环境中加载

2. **修改head.html文件**：

   * 在CSS样式之后，其他JavaScript代码之前引入百度统计partial

   * 具体位置在第28行 `{{ partialCached "head-css.html" . "head-css-cache-key" -}}` 之后

   * 确保代码在jQuery等其他脚本之前加载

3. **代码内容**：

   ```html
   {{ if hugo.IsProduction }}
   <script>
   var _hmt = _hmt || [];
   (function() {
     var hm = document.createElement("script");
     hm.src = "https://hm.baidu.com/hm.js?b56eb3f41045eaa73da163337cd0bd1a";
     var s = document.getElementsByTagName("script")[0]; 
     s.parentNode.insertBefore(hm, s);
   })();
   </script>
   {{ end }}
   ```

4. **验证步骤**：

   * 本地测试：`hugo server -e production`

   * 检查网络请求，确认hm.js已加载

   * 部署后登录百度统计后台验证安装状态

**实施步骤**：

1. 创建 `layouts/partials/baidu_analytics.html` 文件
2. 在 `head.html` 文件中引入该partial
3. 测试代码加载情况
4. 部署并验证安装状态

